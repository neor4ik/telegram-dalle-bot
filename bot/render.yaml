services:
  - type: web
    name: telegram-art-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        fromService: user-provided
      - key: OPENAI_API_KEY
        fromService: user-provided
